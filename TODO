* auto_label (day,week,month ...) : doing
* config file (json) : doing
* work with schedule : doing
* exception and error handle : doing
* work with plug&play media
* support remote backup (ssh)
* comment
* log
* dry run
* code better
